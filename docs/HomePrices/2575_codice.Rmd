---
title: "Codice R"
subtitle: "Home sales prices"
author: "Aldo Solari, matricola 2575"
output: pdf_document
---


# Codice R riproducibile

Includere **solo** il codice indispensabile per ottenere la previsione finale.

```{r, warning=FALSE, message=FALSE}
library(readr)
library(tidymodels)
PATH <- "https://raw.githubusercontent.com/aldosolari/DM/master/docs/HomePrices/"
train = read_csv2(paste0(PATH,"home_prices_train.csv"))
test = read_csv2(paste0(PATH,"home_prices_test.csv"))
fit <- linear_reg() %>% set_engine("lm") %>% fit(price ~ ., data = train)
yhat <- predict(fit, new_data = test) %>% print(n=5)
# scrittura file .TXT
# write.table(file="2575.txt", yhat$.pred, row.names = FALSE, col.names = FALSE)
```

\newpage

```{r}
sessionInfo()
```


